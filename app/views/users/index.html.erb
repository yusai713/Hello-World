<%= link_to "投稿画面に戻る","/users/new" %>

<% @users.each do |user| %>
<p>
  <%= user.name %>
  <%= user.age %>
  <%= link_to '編集', "users/#{user.id}/edit", method: :get %>
  <%= link_to '削除', "/users/#{user.id}", method: :delete, data: {confirm: "本当に削除しますか?"}  %>
</p>
<% end %>
